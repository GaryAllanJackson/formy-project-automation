First, update hardcoded paths for config file, log file(PageHelper.java), and the Chrome, Firefox and PhantomJs drivers.
Second, mark the test folder as a test folder.
Third, mark pages and Entities as source root folder.
Fourth, make sure that your java version is 1.8 so that you can compile.


New things to implement:
Writing the test results in a readable 

Change configuration file name to ConfigurationSetup.(txt|tscript)
Option to select a folder where test files are instead of individual files - 
	Possibly numbering files for run order
	Sort files when taken in

